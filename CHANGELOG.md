# himawari8-observer 更新记录

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

软件版本以 `src/metadata/soft_info.py` 的 `SOFTWARE_VERSION` 为准（CLI `-v`、托盘关于）。

## [Unreleased]

### 修复

- 瓦片下载改为先写 `.part` 再原子替换；跳过已存在文件时校验 PNG 头，避免半写入残片被当成已下载。

## [1.4.0] - 2026-09-03

### 新增

- 托盘菜单顶部显示当前壁纸观测时间（本机时区 + UTC）；尚未成功应用时显示「尚未应用」。
- 首次启动与托盘「立即更新壁纸」在目标分辨率高于 2200 时先以 2200（4d）上墙，再拉目标档。
- 瓦片下载首轮失败后，对未成功项再补下最多 2 轮（失败重试队列）。
- 托盘「关于」可检查 GitHub Releases 是否有新版本；已最新则提示，有更新则可打开下载页。
- 托盘可改项（分辨率、修边、边距、清理开关）写入程序目录 `settings.json`，重启后保留；显式 CLI 参数仍可覆盖文件值。
- 日志可开关（托盘「启用日志」/ `--logging`），默认关闭；状态写入 `settings.json`。

### 变更

- 瓦片下载连接池与并发线程数对齐；本地已有非空瓦片文件时跳过重复下载。
- 默认分辨率改为最高档 `11000`（20d）；默认顶边黑边改为 `0%`（底边仍为 `5%`）。

## [1.3.1] - 2026-09-03

### 新增

- 设壁纸成功后默认清理瓦片与旧观测目录，仅保留当前壁纸文件；可用 `--no-cleanup-after-apply` / 托盘关闭。
- CLI 与托盘可分别配置顶边 / 底边黑边百分比（`--margin-top` / `--margin-bottom`）。
- 观测时间与成图参数（档位、修边开关与边距）未变化时跳过重复下载。
- 托盘与可执行文件改用静态地球图标资源（`assets/`）。
- README 增加徽章与图示，并提供繁体中文、英文译本。

### 变更

- 黑边修边默认开启；修正修边粘贴坐标。
- 运行日志改为英文，并为下载、设壁纸、清理、注册表启动项、托盘图标加载等失败路径补充记录。
- 常驻入口收拢到 `src.app`；`run.py` 仅作薄启动层。
- 模块与标识命名整理：`tray/menu.py`、`pic/pic.py`、`scheduler.py`；去掉 `cls_` / `arr_` / `dl_` 前缀；Pic 域与 `resolution_grade` 对齐。
- 包内聚：`wallpaper/`、`download/`，以及 `folder` / `set_wallpaper` / `cleanup` 分文件。

### 修复

- 「关于」对话框改为独立线程弹出，避免卡住托盘消息循环导致死锁。

### 移除

- 未使用的遗留模块与死代码路径；废弃的 `parse_known_args` 桩与无用注释。

## [1.3.0] - 2026-07-19

> 版本号早在 2024-08-05 自 1.2.0 升为 1.3.0；本段归纳其后至 2026-07 落地、且仍属 1.3.0 标签下的用户可见能力。

### 新增

- 托盘：手动更新壁纸、暂停/恢复定时更新、切换分辨率、打开日志、开机启动勾选状态与注册表同步。
- CLI `-r` / `--resolution` 接入流水线；集中档位映射（含补全 `2d` / 1100px）。
- CLI `-a` / `--adjust` 经任务装配冻结后进入合成与设壁纸路径。
- 壁纸更新流水线（可注入适配器）；托盘与定时器共用互斥触发入口（`WallpaperJobRef`）。
- 瓦片下载统一走 `download_tiles`（会话重试）；合成前校验瓦片是否下全。
- 进程启动后立即执行一次壁纸更新（与周期调度一致）。
- CLI 参数说明文档（`doc/cli-arguments.md`）。

### 变更

- 仅保留等分瓦片路径；分辨率档位与像素边长以 `resolution_grade` 为单一 seam。
- 工程侧引入 ruff / 提交脚本等维护约定。

### 移除

- 完整图（complete）下载路径与 `-dl` 选项。
- Pic 构造时不再生成 complete 下载相关字段。

## [1.2.0] - 2024-08-02

### 变更

- 托盘菜单与 CLI 参数解析增强；下载线程与配置整理。
- 版本号调整为 1.2.0。

## [0.0.1] - 2021-05-18

### 新增

- 项目初版：定时拉取葵花 8 号影像并设为 Windows 桌面壁纸。
