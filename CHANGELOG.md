# himawari8-observer 更新记录

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

软件版本以 `src/metadata/app_info.py` 的 `SOFTWARE_VERSION` 为准（CLI `-v`、托盘关于）。

## [Unreleased]

### 新增

- 可选「显示台风位置」（`settings.json` / CLI `--show-typhoon-marker` / 托盘），默认关闭；有 NICT D531108 台风中心时在壁纸上标注，失败则静默跳过；写入成图指纹第 7 项。

### 变更

- 切换台风标注时，若观测时间与其它成图参数未变且无标记底图仍在，则只叠加/复原标记，不再整轮重下瓦片；且**不先请求** `latest.json`，避免网络超时拖慢开关。开启时仅当存在**同观测时间**的台风中心缓存时才画点（缓存来自上次全量更新成功拉取）。
- 切换「减轻色带」时，若底图仍在则从 ``*_base``（未去色带）重建成品，不再重下瓦片、不拉 `latest.json`；台风开关与色带开关共用此后处理快路径。
- 切换修边/边距时，若等分圆盘 ``*_disk`` 仍在则从中间图重算修边（再按需去色带/台风），不再重下瓦片、不拉 `latest.json`。

### 修复

- 自动跟 `latest.json` 时，若观测时间早于已成功应用的时间则跳过更新，避免源站回退导致壁纸往回刷。
- 换参重建 job 时不再替换 `applied_run_state` 字典对象，避免下载中途开关台风导致完成指纹写到旧对象、persist 读新对象，从而反复全量重下。

## [1.5.0] - 2026-09-04

### 新增

- 将上次成功应用的指纹与壁纸路径写入 `settings.json`；指纹相同且桌面仍是该文件则跳过，桌面被手动更换则仅重设壁纸。
- 托盘悬停（`icon.title`）显示当前壁纸本地/UTC 观测时间与实际上墙分辨率（含渐进预览档）；菜单内本地/UTC 两项仍保留。
- 渐进预览档上墙成功后即刷新托盘展示时间（仍不写跳过指纹）；目标档完成后再正式落盘。
- 可选「按本地钟点使用昨日影像」（`settings.json` / CLI `--use-yesterday-local-time` / 托盘），默认关闭；开启后按本机当前钟点减一天并换算 UTC（向下取整到 10 分钟）取图，不读 `latest.json`。
- 可选「减轻色带」（`settings.json` / CLI `--reduce-banding` / 托盘），默认关闭；写入成图指纹。
- 开机启动偏好写入 `settings.json`（`startup_enabled`），默认关闭。

### 修复

- 上墙成功后调用 `icon.update_menu()`，避免 Windows 托盘菜单缓存导致时间文案不刷新。
- 瓦片下载改为先写 `.part` 再原子替换；跳过已存在文件时校验 PNG 头，避免半写入残片被当成已下载。
- 渐进预览轮不再清理缓存、不写入跳过指纹，减少预览档瓦片被删后目标档重下。
- 等分合成与修边在贴图后及时关闭瓦片/画布，降低高分辨率时的内存峰值重叠。
- 开启修边时瓦片直接贴到修边画布并一次落盘，避免先合成正方形再编解码的额外开销。
- 修边取屏尺寸改为 Win32 ``GetSystemMetrics``，不再依赖 ``ImageGrab`` 截屏。

### 变更

- 默认分辨率改为 `2200`（4d）。
- 补充调度首跑渐进、观测时间解析、桌面壁纸 API、修边几何与指纹灌回/成品缺失等单测。
- 模块整理：`soft_*` → `app_config` / `app_info`；`update_check` → `version_check`；`cleanup` / `folders` / `desktop` 收进 `wallpaper/`；压平 `pic` / `event` / `log`；一次性入口 `main` → `oneshot`；托盘拆为 `menu` + `actions`。
- 日志降噪：压制 urllib3/Pillow DEBUG；瓦片成功改为汇总 INFO；开机启动探测与 settings 载荷降为 DEBUG；文件日志改为滚动（2MB×3）。

## [1.4.0] - 2026-09-03

### 新增

- 托盘菜单顶部显示当前壁纸观测时间（本机时区 + UTC）；尚未成功应用时显示「尚未应用」。
- 首次启动与托盘「立即更新壁纸」在目标分辨率高于 2200 时先以 2200（4d）上墙，再拉目标档。
- 瓦片下载首轮失败后，对未成功项再补下最多 2 轮（失败重试队列）。
- 托盘「关于」可检查 GitHub Releases 是否有新版本；已最新则提示，有更新则可打开下载页。
- 托盘可改项（分辨率、修边、边距、清理开关）写入程序目录 `settings.json`，重启后保留；显式 CLI 参数仍可覆盖文件值。
- 日志可开关（托盘「启用日志」/ `--logging`），默认关闭；状态写入 `settings.json`。

### 变更

- 瓦片下载连接池与并发线程数对齐；本地已有非空瓦片文件时跳过重复下载。
- 默认分辨率改为最高档 `11000`（20d）；默认顶边黑边改为 `0%`（底边仍为 `5%`）。

## [1.3.1] - 2026-09-03

### 新增

- 设壁纸成功后默认清理瓦片与旧观测目录，仅保留当前壁纸文件；可用 `--no-cleanup-after-apply` / 托盘关闭。
- CLI 与托盘可分别配置顶边 / 底边黑边百分比（`--margin-top` / `--margin-bottom`）。
- 观测时间与成图参数（档位、修边开关与边距）未变化时跳过重复下载。
- 托盘与可执行文件改用静态地球图标资源（`assets/`）。
- README 增加徽章与图示，并提供繁体中文、英文译本。

### 变更

- 黑边修边默认开启；修正修边粘贴坐标。
- 运行日志改为英文，并为下载、设壁纸、清理、注册表启动项、托盘图标加载等失败路径补充记录。
- 常驻入口收拢到 `src.app`；`run.py` 仅作薄启动层。
- 模块与标识命名整理：`tray/menu.py`、`pic/pic.py`、`scheduler.py`；去掉 `cls_` / `arr_` / `dl_` 前缀；Pic 域与 `resolution_grade` 对齐。
- 包内聚：`wallpaper/`、`download/`，以及 `folder` / `set_wallpaper` / `cleanup` 分文件。

### 修复

- 「关于」对话框改为独立线程弹出，避免卡住托盘消息循环导致死锁。

### 移除

- 未使用的遗留模块与死代码路径；废弃的 `parse_known_args` 桩与无用注释。

## [1.3.0] - 2026-07-19

> 版本号早在 2024-08-05 自 1.2.0 升为 1.3.0；本段归纳其后至 2026-07 落地、且仍属 1.3.0 标签下的用户可见能力。

### 新增

- 托盘：手动更新壁纸、暂停/恢复定时更新、切换分辨率、打开日志、开机启动勾选状态与注册表同步。
- CLI `-r` / `--resolution` 接入流水线；集中档位映射（含补全 `2d` / 1100px）。
- CLI `-a` / `--adjust` 经任务装配冻结后进入合成与设壁纸路径。
- 壁纸更新流水线（可注入适配器）；托盘与定时器共用互斥触发入口（`WallpaperJobRef`）。
- 瓦片下载统一走 `download_tiles`（会话重试）；合成前校验瓦片是否下全。
- 进程启动后立即执行一次壁纸更新（与周期调度一致）。
- CLI 参数说明文档（`doc/cli-arguments.md`）。

### 变更

- 仅保留等分瓦片路径；分辨率档位与像素边长以 `resolution_grade` 为单一 seam。
- 工程侧引入 ruff / 提交脚本等维护约定。

### 移除

- 完整图（complete）下载路径与 `-dl` 选项。
- Pic 构造时不再生成 complete 下载相关字段。

## [1.2.0] - 2024-08-02

### 变更

- 托盘菜单与 CLI 参数解析增强；下载线程与配置整理。
- 版本号调整为 1.2.0。

## [0.0.1] - 2021-05-18

### 新增

- 项目初版：定时拉取葵花 8 号影像并设为 Windows 桌面壁纸。
